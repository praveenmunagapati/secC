#include <stdbool.h>
#include <stdio.h>

int main(void) {
    setbuf(stdout,0);
    //// a + - / * %
    // inc dec ++ --
    // = ,
    // += -= *= /= a+=1 = a =a +1
    // relational > < >= <= != ==
    //logical && || !
    //bitwise & | ^ ~ <<  >>

    
    return 0;
}

#include <stdio.h>
#include "mylib.h"

int main(void) {
  setbuf(stdout, 0);
   int total = sids(333);
    printf("%d\n",total);
    isprime(17);
    isprime(107);
    isprime(170);
    isprime(174);

}